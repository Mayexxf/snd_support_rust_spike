=== Машина ===
  процессор               Intel(R) Xeon(R) CPU E5-2696 v2 @ 2.50GHz
  архитектура             x86_64, ядер 4
  наборы инструкций       SSE4.2 есть, AVX есть
  система                 Windows 10 Pro 22H2 (сборка 19045)
  гипервизор              Microsoft Hv

  ⚠ ЦИФРЫ НЕ ПЕРЕНОСЯТСЯ НА ЦЕЛЕВУЮ МАШИНУ:
    · виртуальная машина (Microsoft Hv). Захват экрана и работа с GPU здесь ведут себя иначе, чем на живом железе
    · у процессора есть AVX, а у целевого Braswell его нет. Программный кодер здесь получит фору, которой на целевой машине не будет
    Этот прогон проверяет, что стенд работает, а не что решение проходит.
  сборка стенда           2baac45+правки

=== Источник ===
  DXGI Desktop Duplication, 1024×768, адаптер «Microsoft Basic Render Driver»
  разрешение таймера поднято до 1 мс

  ⚠ адаптер «Microsoft Basic Render Driver» — программный растеризатор, настоящего GPU нет. Строка «копирование в память» меряет память, а не шину GPU

Прогон 25 с при цели 30 к/с, копирование: только изменившиеся области.
Не трогайте мышь, если меряете статичный стол; крутите документ, если прокрутку.

Кодирование: VP9 в 512×384 при 2000 кбит/с, cpu-used 8, потоков 4.
Кадр уменьшен в 2 раза: 512×384, то есть 25% пикселей.

=== Захват · DXGI Desktop Duplication, 1024×768, адаптер «Microsoft Basic Render Driver» ===
Разрешение 1024×768, цель 30 к/с, длительность 25.0 с

-- Кадры --
  опрошено                740
  с новым содержимым      738 (29.5 к/с фактически)
  экран не менялся        0.3% опросов
  менялось за кадр        78.95% площади, прямоугольников в среднем 1.4
  копировалось за кадр    78.95% площади

-- Стоимость кадра, мс --
                            p50    p95    p99    max
  работа захвата          0.1    0.2    1.1    1.7
  копирование в память   17.1   32.4   34.7   39.5
  конвертация в YUV       0.7    1.8    2.8    9.8
  кодирование             7.5   11.9   17.5   40.3
  ИТОГО на кадр          25.8   41.4   44.9   53.6

  ожидание кадра          6.2   23.0   26.8   32.5
  (ожидание — не расход: при цели 30 к/с так и должно быть)

-- Бюджет кадра --
  интервал при 30 к/с      33.3 мс
  занято p50 / p95        77% / 124%
  ✗ НЕ УСПЕВАЕТ. Целевая частота недостижима в этой конфигурации

-- Поток --
  средний битрейт         2.09 Мбит/с
  ключевой кадр           42 КБ (3 шт.)
  разностный кадр         8706 Б (735 шт.)

-- Процессор --
  время CPU               15.53 с (польз. 15.02 + сист. 0.52)
  занято ядер            0.62 из 4 (15.5% машины)

